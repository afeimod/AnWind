--- a/Xtranssock.c
+++ b/Xtranssock.c
@@ -200,20 +200,20 @@
 
 
 #if defined(X11_t)
-#define UNIX_PATH "/tmp/.X11-unix/X"
-#define UNIX_DIR "/tmp/.X11-unix"
+#define UNIX_PATH "@APP_PREFIX@/tmp/.X11-unix/X"
+#define UNIX_DIR "@APP_PREFIX@/tmp/.X11-unix"
 #endif /* X11_t */
 #if defined(XIM_t)
-#define UNIX_PATH "/tmp/.XIM-unix/XIM"
-#define UNIX_DIR "/tmp/.XIM-unix"
+#define UNIX_PATH "@APP_PREFIX@/tmp/.XIM-unix/XIM"
+#define UNIX_DIR "@APP_PREFIX@/tmp/.XIM-unix"
 #endif /* XIM_t */
 #if defined(FS_t) || defined(FONT_t)
-#define UNIX_PATH "/tmp/.font-unix/fs"
-#define UNIX_DIR "/tmp/.font-unix"
+#define UNIX_PATH "@APP_PREFIX@/tmp/.font-unix/fs"
+#define UNIX_DIR "@APP_PREFIX@/tmp/.font-unix"
 #endif /* FS_t || FONT_t */
 #if defined(ICE_t)
-#define UNIX_PATH "/tmp/.ICE-unix/"
-#define UNIX_DIR "/tmp/.ICE-unix"
+#define UNIX_PATH "@APP_PREFIX@/tmp/.ICE-unix/"
+#define UNIX_DIR "@APP_PREFIX@/tmp/.ICE-unix"
 #endif /* ICE_t */
