# Project-wide Gradle settings.
# v2.22.5 CI OOM 修复：2G 堆在 compressReleaseAssets（zipflinger 对大
# 资产整文件 deflate）+ Kotlin/Java 并行编译下会 OutOfMemoryError，
# 提到 4G（GitHub ubuntu-latest 16GB 内存，安全），并给 Metaspace 上限。
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# AndroidX
android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin
kotlin.code.style=official
